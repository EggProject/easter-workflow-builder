{
  "name": "server",
  "version": "0.0.0",
  "type": "module",
  "private": true,
  "scripts": {
    "typecheck": "tsc --noEmit",
    "lint": "eslint ."
  },
  "dependencies": {
    "agent": "workspace:*",
    "core": "workspace:*",
    "db": "workspace:*",
    "engine": "workspace:*",
    "logger": "workspace:*",
    "protocol": "workspace:*",
    "providers": "workspace:*"
  }
}
