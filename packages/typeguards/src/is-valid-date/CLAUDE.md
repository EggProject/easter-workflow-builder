# packages/typeguards/src/is-valid-date

`isValidDate(value?: unknown): value is Date` - `Date` példány ÉS érvényes dátum (nem
`Invalid Date`, azaz `!Number.isNaN(value.getTime())`).

A csomag konvencióit lásd a szülő [`../../CLAUDE.md`](../../CLAUDE.md)-ben.
