# packages/typeguards/src/is-float

`isFloat(n?: unknown): n is number` - véges szám törtrésszel (`n % 1 !== 0`). Kizárja a
`NaN`-t és a végtelent.

A csomag konvencióit lásd a szülő [`../../CLAUDE.md`](../../CLAUDE.md)-ben.
