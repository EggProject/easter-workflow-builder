# packages/typeguards/src/is-object

`isObject(item?: unknown): item is object` - objektum, kizárva a `null`-t és a tömböket.

A csomag konvencióit lásd a szülő [`../../CLAUDE.md`](../../CLAUDE.md)-ben.
