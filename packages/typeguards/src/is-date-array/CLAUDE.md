# packages/typeguards/src/is-date-array

`isDateArray(dates: unknown, expectedLength?: number): dates is Date[]` - tömb, aminek minden
eleme `Date` példány, opcionálisan a tömb hosszát is ellenőrzi.

A csomag konvencióit lásd a szülő [`../../CLAUDE.md`](../../CLAUDE.md)-ben.
