/**
 * A REST végpontok közös útvonal előtagja (SPEC-005 4.1 szekció). Az SSE stream
 * (`STREAM_PATH`, `stream-path.ts`) szándékosan NEM ez alatt áll, lásd ott az indokot.
 */
export const API_BASE_PATH = '/api';
